import { LoginForm } from "@/components/auth/login-form"

export const metadata = {
  title: "Đăng nhập | Photo Scout",
  description: "Đăng nhập vào tài khoản của bạn",
}

export default function LoginPage() {
  return <LoginForm />
}
