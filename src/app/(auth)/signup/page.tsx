import { SignupForm } from "@/components/auth/signup-form"

export const metadata = {
  title: "Đăng ký | Photo Scout",
  description: "Tạo tài khoản mới",
}

export default function SignupPage() {
  return <SignupForm />
}
